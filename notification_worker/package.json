{
  "name": "craycare-notification-worker",
  "version": "1.1.0",
  "description": "Monitors Firebase RTDB sensor data and sends FCM push notifications when thresholds are exceeded",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
